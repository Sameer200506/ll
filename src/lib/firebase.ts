// Stub — Firebase is only used when real credentials are provided.
// The app runs in demo mode using localStorage when credentials are missing.
export const auth = null as any;
export const db = null as any;
